input {
    tcp {
        host => "localhost"
        port => 5000
        mode => server
        codec => json_lines {}
    }
}

## Add your filters / logstash plugins configuration here

output {
	elasticsearch {
		hosts => "elasticsearch:9200"
		user => "elastic"
		password => "changeme"
	}
}
